# Session Configuration
# Generate a secure session secret using: npm run generate-secrets

SESSION_SECRET=your-random-session-secret-key-at-least-32-characters

# JWT Configuration (optional, has default)
# Generate a different secret from SESSION_SECRET

JWT_SECRET=your-secret-key-for-development

# Email Configuration (SendGrid)
# Get your API key from https://app.sendgrid.com/settings/api_keys

SENDGRID_API_KEY=your-sendgrid-api-key

# Application Configuration

NODE_ENV=development

BASE_URL=http://localhost:5000

PORT=5000

# Database Configuration
# Option 1: Use DATABASE_URL (recommended)
DATABASE_URL=postgresql://epml_user:epml_local_password_123@localhost:5432/epml

# Option 2: Use individual PostgreSQL variables (alternative to DATABASE_URL)
PGUSER=epml_user
PGHOST=localhost
PGPASSWORD=epml_local_password_123
PGDATABASE=epml
PGPORT=5432
